# Absolute URL of the backend API. Leave empty locally to go through the Vite dev proxy.
# Example: https://api.example.com
VITE_API_BASE_URL=

# Must match SECRET_SERVICE_KEY on the backend. Sent as x-service-key header on every API call.
SECRET_SERVICE_KEY=
